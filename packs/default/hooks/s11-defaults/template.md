## 默认行为（对标 clowder-ai S11）

未明确指定时的默认行为设置：

- **代码风格**：遵循现有代码风格，`go build ./...` 和 `go test ./...` 必须通过
- **文件大小**：200 行警告 / 350 行硬限
- **提交**：不主动提交，除非明确要求
- **文档**：不主动创建文档文件，除非明确要求
- **错误处理**：信任内部代码和框架保证，不为不可能的场景加错误处理
- **抽象**：不为一次性操作创建抽象

> 当 packBlocks.defaultsBlock 存在时注入具体默认值覆盖。
