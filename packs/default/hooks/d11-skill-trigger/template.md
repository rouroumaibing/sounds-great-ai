## 技能触发（对标 clowder-ai D11）

当前匹配的 skill：{{.MatchedSkill}}
触发原因：{{.TriggerReason}}

技能在 manifest ≠ 在认知路径。坏直觉 → 该用的 skill：
- 想发一堆文字/日志/步骤 → rich-messaging
- 改前端 UI 想看效果 → browser-preview
- 需要图 → image-generation
- 想说"文件在 X 路径" → workspace-navigator
- 架构决定/bug 死磕/多视角 → collaborative-thinking
- 压缩后失忆/找旧决策 → memory-navigation
